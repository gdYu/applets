+ 基础配置
    ```js
    module.export = {  
      // 入口起点。webpack会找出哪些模块和库是入口起点依赖的。
      entry:'./path/entry/file.js' // 默认值 './src/index.js'
      // 多个入口
      // entry:{
      //   app: './src/main.js',
      //   ngapp: './angularjs/main.js'
      //}
      
    }  
  
    ```